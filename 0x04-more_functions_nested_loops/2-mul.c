#include "main.h"
/**
 * mul - Multiples of two integers
 * @t: The first number to multiply
 * @o: The second number to multiply with the first number
 * Return: The result of multiplication between t param and o param
 */
int mul(int t, int o)
{
return (t * o);
}
